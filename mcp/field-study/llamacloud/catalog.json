{
  "tools": [
    {
      "name": "getUploadUrl",
      "description": "Get a pre-signed URL to upload a file to the LlamaParse S3 storage. Not available when authenticating with an API key — use uploadFileByUrl instead.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "purpose": {
            "type": "string",
            "description": "Expected downstream processing workload for the file to upload. Allowed values: 'user_data', 'parse', 'extract', 'split', 'classify', 'sheet', 'agent_app'. Defaults to 'parse' if not provided."
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Get Upload URL",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "uploadFileByUrl",
      "description": "Upload a file to LLamaParse S3 storage providing a URL to download the file data. On upload completion, the file will be sent to LlamaParse S3 storage, so that it can be used for downstream processing tasks like parsing, classification or splitting.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "URL of the file to upload"
          },
          "fileName": {
            "type": "string",
            "description": "Basename of the original file"
          },
          "fileType": {
            "type": "string",
            "description": "Mimetype of the file. Defaults to application/pdf if not provided. Highly recommended to always provide it"
          },
          "purpose": {
            "type": "string",
            "description": "Expected downstream processing workload. Allowed values: 'user_data', 'parse', 'extract', 'split', 'classify', 'sheet', 'agent_app'. Defaults to 'parse' if not provided."
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "url",
          "fileName"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Upload File by URL",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "getUserProjects",
      "description": "List the projects available to the user, with their names, so you can pass the right projectId to other tools. Use this whenever a tool needs a projectId and the correct project is not already known — pick by name, and ask the user if the name is ambiguous.",
      "inputSchema": {
        "type": "object",
        "properties": {
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "List Projects",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "parseFile",
      "description": "Parse a file providing its file ID, retrieving markdown or plain text content of the file. Use with file IDs obtained with the getUploadUrl/uploadFileByUrl tool or that the user provided",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileId": {
            "type": "string",
            "description": "ID of the file to parse, as returned by the file upload tool or provided by the user"
          },
          "tier": {
            "type": "string",
            "enum": [
              "cost_effective",
              "agentic",
              "agentic_plus"
            ],
            "description": "Parsing mode to use. While agentic or agentic_plus are recommended, this parameter defaults to cost_effective if not specified."
          },
          "version": {
            "anyOf": [
              {
                "type": "string",
                "const": "latest"
              },
              {
                "type": "string"
              }
            ],
            "description": "API version to use. Defaults to `latest` if not specified."
          },
          "markdown": {
            "type": "boolean",
            "description": "Whether to extract markdown or plain text. Defaults to true (extract markdown)."
          },
          "pages": {
            "type": "array",
            "items": {
              "type": "number"
            },
            "description": "Specific pages to limit the parsing operation to"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "fileId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Parse File",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "classifyFile",
      "description": "Classify a file (based on specific categories) providing its file ID. Use with file IDs obtained with the getUploadUrl/uploadFileByUrl tool or that the user provided",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileId": {
            "type": "string",
            "description": "ID of the file to classify, as returned by the file upload tool or provided by the user"
          },
          "mode": {
            "type": "string",
            "const": "FAST",
            "description": "Classification mode to use."
          },
          "categories": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "description": "Category identifier"
                },
                "description": {
                  "type": "string",
                  "description": "Category description"
                }
              },
              "required": [
                "type",
                "description"
              ],
              "additionalProperties": false
            },
            "description": "Array of categories for the file to be classfied as. Category types should be lowercase and use snake_case. Category descriptions should be exaustive but not longer than 500 characters"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "fileId",
          "categories"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Classify File",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "splitFile",
      "description": "Split a file into category-based segments providing its file ID. Use with file IDs obtained with the getUploadUrl/uploadFileByUrl tool or that the user provided",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileId": {
            "type": "string",
            "description": "ID of the file to split, as returned by the file upload tool or provided by the user"
          },
          "allowUncategorized": {
            "type": "string",
            "enum": [
              "omit",
              "include",
              "forbid"
            ],
            "description": "Whether to omit, include or forbid uncategorized results. If you forbid uncategorized results, you force categorization even when the confidence is low. Defaults to `include`"
          },
          "categories": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "name": {
                  "type": "string",
                  "description": "Category identifier"
                },
                "description": {
                  "type": "string",
                  "description": "Category description"
                }
              },
              "required": [
                "name",
                "description"
              ],
              "additionalProperties": false
            },
            "description": "Array of categories for the file to be classfied as. Category names should be lowercase and use snake_case. Category descriptions should be exaustive but not longer than 500 characters"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "fileId",
          "categories"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Split File",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "searchSchemaTemplates",
      "description": "Search the built-in library of starter extraction schemas (invoice, contract, resume, 10-K, patient intake and more) by keyword or category. Returns matching templates with their top-level field names, but not the full JSON Schema — call `getSchemaTemplate` for that. Prefer this over `generateExtractionConfig` when the user wants a common document type: it is instant and needs no sample file.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Keywords to match against template names, descriptions, categories and field names, e.g. \"invoice\", \"legal agreement\", \"line_items\". Omit to list everything."
          },
          "category": {
            "type": "string",
            "description": "Restrict to one category. Allowed values: business, legal, healthcare, finance_research, education."
          },
          "limit": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "description": "Maximum number of templates to return. Defaults to 10."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Search Schema Templates",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "getSchemaTemplate",
      "description": "Retrieve the full JSON Schema for one starter extraction template, by id, as returned by `searchSchemaTemplates`. Pass the schema to `createExtractionConfigFromSchema` — edit it first if the user needs extra or fewer fields.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "templateId": {
            "type": "string",
            "description": "ID of the template, as returned by `searchSchemaTemplates`, e.g. \"invoice\" or \"ten_k\"."
          }
        },
        "required": [
          "templateId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Get Schema Template",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "createExtractionConfigFromSchema",
      "description": "Create an extraction configuration from a JSON Schema you already have, and return its configuration id for use with `extractFile`. Supply either a `templateId` from `searchSchemaTemplates` (the server loads the schema for you) or an explicit `dataSchema` — a schema you wrote, or a template schema you edited. Unlike `generateExtractionConfig`, this needs no sample file and involves no LLM step.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "templateId": {
            "type": "string",
            "description": "ID of a starter template to use verbatim, as returned by `searchSchemaTemplates`. Provide this or `dataSchema`, not both."
          },
          "dataSchema": {
            "type": "object",
            "additionalProperties": {
            },
            "description": "An explicit JSON Schema object describing the fields to extract. Must be an object schema with a `properties` map. Provide this or `templateId`, not both."
          },
          "name": {
            "type": "string",
            "description": "Human-readable name for the configuration. Defaults to the template title, or \"extraction-config\" for an explicit schema."
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          },
          "tier": {
            "type": "string",
            "enum": [
              "cost_effective",
              "agentic"
            ],
            "description": "Extraction quality tier. Defaults to cost_effective; use agentic for complex or ambiguous documents."
          },
          "extractionTarget": {
            "type": "string",
            "enum": [
              "per_doc",
              "per_page",
              "per_table_row"
            ],
            "description": "Whether to produce one object per document (default), per page, or per table row."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Create Extraction Config From Schema",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "generateExtractionConfig",
      "description": "Generate the configuration to extract structured data from a specific file using the Extract service from the LlamaParse Platform. Provide a prompt describing what the schema of the extracted data, the ID of the file to extract and, optionally, a project ID.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileId": {
            "type": "string",
            "description": "ID of the file for which to generate the extraction config, as returned by the file upload tool or provided by the user"
          },
          "generationPrompt": {
            "type": "string",
            "description": "Prompt to generate the extraction configuration, describing the data schema to extract."
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "fileId",
          "generationPrompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Generate Extraction Config",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "extractFile",
      "description": "Extract structured data from a file based on the configuration created with the `generateExtractionConfig` tool. Returns the extracted structured data.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileId": {
            "type": "string",
            "description": "ID of the file to extract, as returned by the file upload tool or provided by the user"
          },
          "configurationId": {
            "type": "string",
            "description": "ID of the configuration to use to extract data from the file, as provided by the `generateExtractionConfig` tool."
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "fileId",
          "configurationId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Extract Structured Data",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "listIndexes",
      "description": "List all the available indexes on the LlamaParse Platform. Indexes are vector-indexed directories with the possibility of searching/reading/grepping files and performing retrieval.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "List Indexes",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "findFilesInIndex",
      "description": "Search files within an index. Optionally provide the file name to filter for or a substring that should be contained in the file name",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileName": {
            "type": "string",
            "description": "Extact match for the file name to search for"
          },
          "fileNameContains": {
            "type": "string",
            "description": "Substring contained in the file name to search for (recommended using over fileName)"
          },
          "indexId": {
            "type": "string",
            "description": "Index ID, as provided by the listIndexes tool"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "indexId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Find Files in Index",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "readFileFromIndex",
      "description": "Read the content of a file from an index, providing its file ID and, optionally, an offset and a maximum length (in characters) to read.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileId": {
            "type": "string",
            "description": "ID of the file to read, as obtained by the searchFilesFromIndex tool"
          },
          "offset": {
            "type": "number",
            "description": "Offset (in characters) from which to read the file from"
          },
          "maxLength": {
            "type": "number",
            "description": "Maximum length (in characters) to read starting from the offset."
          },
          "indexId": {
            "type": "string",
            "description": "Index ID, as provided by the listIndexes tool"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "fileId",
          "indexId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Read File from Index",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "grepFileFromIndex",
      "description": "Grep the content of a file from an index, providing its file ID, the pattern to grep for and, optionally, a number of context characters and a maximum number of grep matches to retrieve",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileId": {
            "type": "string",
            "description": "ID of the file to read, as obtained by the searchFilesFromIndex tool"
          },
          "pattern": {
            "type": "string",
            "description": "Pattern to grep the file with"
          },
          "contextChars": {
            "type": "number",
            "description": "Context (in characters) to retrieve along with the grep match"
          },
          "limit": {
            "type": "number",
            "description": "Maximum number of grep matches to retrieve"
          },
          "indexId": {
            "type": "string",
            "description": "Index ID, as provided by the listIndexes tool"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "fileId",
          "pattern",
          "indexId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Grep File in Index",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "retrieveFromIndex",
      "description": "Perform hybrid search on the index, providing a query and, optionally, the top K documents to retrieve and the top N documents to rerank",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Query to search for"
          },
          "topK": {
            "type": "number",
            "description": "Top K documents to retrieve. Defaults to 10."
          },
          "rerankTopN": {
            "type": "number",
            "description": "Top N documents to rerank. If not provided, reranking will be disabled."
          },
          "indexId": {
            "type": "string",
            "description": "Index ID, as provided by the listIndexes tool"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "query",
          "indexId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Search Index",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "createDirectory",
      "description": "Create a directory (folder) to hold source documents. A directory is what an index is built over: upload files, add them to a directory with addFilesToDirectory, then call createIndex on it.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Name for the new directory"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID to create the directory in. Required — call getUserProjects to list the available projects and ask the user which one to use if it is not obvious."
          },
          "description": {
            "type": "string",
            "description": "Optional description for the directory"
          }
        },
        "required": [
          "name",
          "projectId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Create Directory",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "listDirectories",
      "description": "List the directories in a project. Returns one page at a time — pass the returned nextPageToken to fetch more. By default only user directories are listed; indexes create their own internal output directories, which are not valid sources for a new index.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Exact directory name to filter by"
          },
          "type": {
            "type": "string",
            "enum": [
              "user",
              "index",
              "ephemeral"
            ],
            "description": "Directory type to list. Defaults to 'user'. Only pass 'index' or 'ephemeral' if you specifically need internal directories."
          },
          "pageSize": {
            "type": "number",
            "description": "Maximum number of directories to return per page"
          },
          "pageToken": {
            "type": "string",
            "description": "Token from a previous response to retrieve the next page"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "List Directories",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "listDirectory",
      "description": "List the files inside a directory, along with the directory itself. Returns one page at a time — pass the returned nextPageToken to fetch more. Use directoryFileId, not fileId, when referring to a file in later calls.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "directoryId": {
            "type": "string",
            "description": "ID of the directory to list, as returned by listDirectories"
          },
          "displayNameContains": {
            "type": "string",
            "description": "Substring match on the file name (case-insensitive)"
          },
          "pageSize": {
            "type": "number",
            "description": "Maximum number of files to return per page"
          },
          "pageToken": {
            "type": "string",
            "description": "Token from a previous response to retrieve the next page"
          },
          "includeDownloadUrls": {
            "type": "boolean",
            "description": "Include a temporary download URL for each file. Defaults to false; only enable it if you need to fetch the file contents."
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "directoryId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "List Directory Contents",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "addFilesToDirectory",
      "description": "Add already-uploaded files to a directory, so they can be indexed. Takes file IDs from getUploadUrl or uploadFileByUrl. Files are added one by one, so the response reports which succeeded and which failed — retry only the failed ones. If the directory already backs an index, call syncIndex afterwards to pull the new files in.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "directoryId": {
            "type": "string",
            "description": "ID of the directory to add the files to"
          },
          "fileIds": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "description": "IDs of the files to add, as returned by getUploadUrl or uploadFileByUrl"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "directoryId",
          "fileIds"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Add Files to Directory",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      }
    },
    {
      "name": "createIndex",
      "description": "Create an index over a directory, making its documents searchable with retrieveFromIndex and the other index tools. The directory must already contain the files you want indexed — upload them and call addFilesToDirectory first. Indexing runs in the background: the returned index is not queryable until getIndexStatus reports it ready.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sourceDirectoryId": {
            "type": "string",
            "description": "ID of the directory holding the documents to index, as returned by createDirectory or listDirectories"
          },
          "name": {
            "type": "string",
            "description": "Name for the index. Defaults to the source directory name if omitted."
          },
          "description": {
            "type": "string",
            "description": "Description of what the index contains. Shown by listIndexes, so it is worth setting — it is how an agent later decides whether this index answers a question."
          },
          "storeAttachments": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "screenshots",
                "items"
              ]
            },
            "description": "Extra artifacts to store per page. 'screenshots' keeps rendered page images; 'items' keeps structured items with bounding boxes. Defaults to ['screenshots']."
          },
          "parseConfigId": {
            "type": "string",
            "description": "Parse configuration to use. Omit to let the platform create a default one."
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "sourceDirectoryId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Create Index",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "getIndexStatus",
      "description": "Check whether an index has finished building. Indexing is asynchronous, so an index created or synced moments ago will not return results yet. Poll this until status is 'ready'; 'failed' means the build did not complete. Querying an index that is not ready looks identical to an index with no matching documents.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "indexId": {
            "type": "string",
            "description": "Index ID, as returned by createIndex or listIndexes"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "indexId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Get Index Status",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "syncIndex",
      "description": "Re-index a directory, picking up files added or changed since the last run. Indexes do not refresh on their own, so this is the only way an existing index sees new documents. Runs in the background — poll getIndexStatus until status is ready. If a sync is already running, this returns syncStarted=false (the underlying API responds 409 or 429): do not retry syncIndex, call getIndexStatus to check the running sync and wait until status is ready.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "indexId": {
            "type": "string",
            "description": "Index ID, as returned by createIndex or listIndexes"
          },
          "projectId": {
            "type": "string",
            "description": "Project ID that the tool should use. Uses the default project if not provided."
          }
        },
        "required": [
          "indexId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Sync Index",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      }
    },
    {
      "name": "parseWithLiteParse",
      "description": "Parse a PDF file with LiteParse, a fast, in-process parser that does not consume credits from the LlamaParse Platform. The tool needs a file ID obtained with the getUploadUrl/uploadFileByUrl tool or provided by the user. Only works with PDF files.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileId": {
            "type": "string",
            "description": "ID of the file to parse."
          },
          "pages": {
            "type": "array",
            "items": {
              "type": "number"
            },
            "description": "Page numbers to limit the parsing operation to. 1-based."
          },
          "markdown": {
            "type": "boolean",
            "description": "Whether the output text should be markdown-formatted or plain text"
          },
          "includeJson": {
            "type": "boolean",
            "description": "Whether to include the JSON array of pages (with bboxes) in the parse result"
          }
        },
        "required": [
          "fileId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Parse File with LiteParse",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    },
    {
      "name": "estimateFileComplexity",
      "description": "Estimate the parsing complexity of a PDF file (providing its file ID) using LiteParse. Returns a JSON object mapping each page with the LlamaParse tier it should be parsed with (or if you should use LiteParse), based on the parsing complexity and the need for OCR. Use in combination with parseFile and parseWithLiteParse. The tool needs a file ID obtained with the getUploadUrl/uploadFileByUrl tool or provided by the user. Only works with PDF files.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fileId": {
            "type": "string",
            "description": "ID of the file whose parsing complexity you want to estimate."
          },
          "includeLayout": {
            "type": "boolean",
            "description": "Whether or not to include layout signals in the complexity estimation. Defaults to false."
          }
        },
        "required": [
          "fileId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "title": "Estimate File Complexity",
        "readOnlyHint": true,
        "destructiveHint": false,
        "openWorldHint": true
      }
    }
  ]
}